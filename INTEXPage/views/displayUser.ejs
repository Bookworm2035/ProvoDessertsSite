<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Users</title>
   <style>
    body {
        font-family: Arial, sans-serif;
    }

    label {
        font-weight: bold;
        font-size: 1.2em;
    }

    table {
        width: 100%;
        background-color: antiquewhite;
        border: 1px solid black;
        border-collapse: collapse;
        margin-top: 20px;
    }

    th, td {
        padding: 10px;
        text-align: left;
    }

    th {
        background-color: cornflowerblue;
        color: white;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    button {
        padding: 8px;
        background-color: #4CAF50;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

    button:hover {
        background-color: #45a049;
    }
</style>
</head>
<body>
   <label><b>User Information:</b></label>
   <br><br>
   <table style="width: 100%; background-color: antiquewhite; border: 1px solid black;">
     <thead style="background-color: cornflowerblue; color: white;">
       <tr>
         <th style="text-align: left;">Username</th>
         <th style="text-align: left;">Password</th>
         <!-- <th style="text-align: left;">Capital</th> -->
         <th></th>
         <th></th>
       </tr>
     </thead>
     <tbody style="font-family: Arial; font-size: 9pt;">
      <!-- Loop through records and populate the table, starting from the second row -->
      <% for (let iCount = 1; iCount < myUser.length; iCount++) { %>
      <tr>
          <!-- Adjust these columns based on your actual column names -->
          <td><%= myUser[iCount].username %></td>
          <td><%= myUser[iCount].password %></td>
          <td style="text-align: center;">
              <form action="/editUser/<%= myUser[iCount].user_id %>" method="GET">
                  <button type="submit">Edit</button>
              </form>
          </td>
          <td style="text-align: center;">
              <form action="/deleteUser/<%= myUser[iCount].user_id %>" method="POST">
                  <button type="submit">Delete</button>
              </form>
          </td>
      </tr>
      <% } %>
  </tbody>
   </table>
   <br><br>
   <form action="/addUser" method="GET">
     <button type="submit">Add User</button>
   </form>
</body>
</html>